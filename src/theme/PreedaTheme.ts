import { createMuiTheme } from "@material-ui/core/styles";

export const preedaTheme = createMuiTheme({
  palette: {
    primary: {
      light: "#d88ae9",
      main: "#C658E0",
      dark: "#9f46b3",
      contrastText: "#fff",
    },
  },
});
